<UserControl x:Class="MyMagicCollection.Caliburn.DeckToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions >
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button cal:Message.Attach="OnOpenDeckFile">Read .dec file</Button>

            <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentCards}" />

            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <CheckBox IsChecked="{Binding IsFoil}" >Is Foil</CheckBox>
                <ComboBox SelectedItem="{Binding Language}" ItemsSource="{Binding AvailableLanguages}"/>
                <Button >Save Deckbox.org CSV</Button>
                <Button Margin="20,0,0,0" >Load/Update Price Data</Button>
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
