<UserControl x:Class="MyMagicCollection.Caliburn.CardDatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:userControls="clr-namespace:MyMagicCollection.Caliburn.UserControls"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <ContentControl x:Name="CardDatabaseSearchViewModel" Grid.ColumnSpan="2"/>
        <userControls:CardGrid Grid.Row="1" CardCollection="{Binding SelectedCards}" SelectedCard="{Binding SelectedCard, Mode=TwoWay}" />
        
        <userControls:CardImage Grid.Row="1" Grid.Column="1"
                                Width="314"
                                Height="447"
                                SelectedCard="{Binding SelectedCard, Mode=TwoWay}"
                                VerticalAlignment="Top"/>
    </Grid>
</UserControl>
