<UserControl x:Class="MyMagicCollection.Caliburn.CollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            
            <Label>Current Collection:</Label>
            <ComboBox Width="250" x:Name="Collections" DisplayMemberPath="CollectionName"/>
            
            <Button x:Name="CreateNewCollection">New</Button>
        </StackPanel>
        
        <StackPanel Grid.Row="1">
            <Label>Collection Details</Label>
            <StackPanel Orientation="Horizontal">
                <Label >Name:</Label>
                <TextBox Width="250" x:Name="SelectedCollectionName" VerticalContentAlignment="Center"/>
            </StackPanel>
        </StackPanel>
        
        <!-- TODO: Grid with card details & search feature-->
    </Grid>
</UserControl>
